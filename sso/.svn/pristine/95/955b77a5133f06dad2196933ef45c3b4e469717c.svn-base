package com.goodidea.sso.service;

import org.springframework.web.multipart.MultipartFile;
import org.springframework.web.multipart.MultipartHttpServletRequest;

import com.goodidea.sso.domin.UploadFile;

/**
 * 
* @ClassName: UploadFileService 
* @Description:上传文件service  
* @author lsg
* @date 2017年8月14日 上午10:17:54 
*
 */
public interface UploadFileService {
	
	
	/**
	 * 文件上传至本地(同步)
	 * 
	 * @param fileType
	 *            文件类型
	 * @param multipartFile
	 *            上传文件
	 * @return 路径
	 */
	UploadFile uploadLocal(String fileType, MultipartFile multipartFile);
	
	
}
